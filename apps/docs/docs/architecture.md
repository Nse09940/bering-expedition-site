# Архитектура

- `Next.js` отвечает за публичный сайт.
- `FastAPI` отдает публичный API, а `SQLAlchemy` описывает доменную модель и доступ к данным.
- `PostgreSQL` хранит экспедиции, этапы, события, источники и маршрутные данные.
- `MinIO` предназначен для хранения изображений и сканов.
